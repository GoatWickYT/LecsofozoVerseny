<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             xmlns:viewModels="clr-namespace:Solution.DesktopApp.ViewModels"
             xmlns:views="clr-namespace:Solution.DesktopApp.Views"
             xmlns:models="clr-namespace:Solution.Core.Models;assembly=Solution.Core"
             xmlns:converters="clr-namespace:Solution.ValidationLibrary.Converters;assembly=Solution.ValidationLibrary"
             xmlns:behaviors="clr-namespace:Solution.DesktopApp.Behaviors"
             x:Class="Solution.DesktopApp.Views.CreateOrEditTeamView"
             x:DataType="viewModels:CreateOrEditTeamViewModel"
             Title="">
    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior Command="{Binding AppearingCommand}" EventName="Appearing"/>
        <toolkit:EventToCommandBehavior Command="{Binding DisappearingCommand}" EventName="Disappearing" />
    </ContentPage.Behaviors>

    <ContentPage.Resources>
        <ResourceDictionary>
            <converters:FirstValidationErrorConverter x:Key="FirstValidationErrorConverter" />
        </ResourceDictionary>
    </ContentPage.Resources>

    <ScrollView>
        <StackLayout>
            <Label Text="{Binding Title}" Style="{StaticResource PageTitle}"/>

            <VerticalStackLayout Style="{StaticResource FormLineContainer}" HorizontalOptions="Center">
                <Label Text="Name" Style="{StaticResource InputLabel}" />
                <Entry Text="{Binding Name.Value}" MinimumWidthRequest="400" MaximumWidthRequest="400">
                    <Entry.Behaviors>
                        <toolkit:EventToCommandBehavior EventName="TextChanged" Command="{Binding TeamNameValidationCommand}" />
                    </Entry.Behaviors>
                </Entry>
                <Label Text="{Binding Name.Errors, Converter={StaticResource FirstValidationErrorConverter}}"
                        Style="{StaticResource ValidationErrorLabelStyle}" />
            </VerticalStackLayout>

            <Border Stroke="Gray" StrokeThickness="2" Padding="20" WidthRequest="550">
                <VerticalStackLayout Style="{StaticResource FormLineContainer}" HorizontalOptions="Center">
                    <Label Text="Participant name" Style="{StaticResource InputLabel}" HorizontalTextAlignment="Center"/>
                    <Entry Text="{Binding Participant.Name.Value}" MinimumWidthRequest="200" MaximumWidthRequest="200">
                        <Entry.Behaviors>
                            <toolkit:EventToCommandBehavior EventName="TextChanged" Command="{Binding ParticipantValidationCommand}" />
                        </Entry.Behaviors>
                    </Entry>
                    <Label Text="{Binding Participant.Name.Errors, Converter={StaticResource FirstValidationErrorConverter}}"
                                Style="{StaticResource ValidationErrorLabelStyle}" HorizontalTextAlignment="Center"/>

                    <Label Text="Participant image" Style="{StaticResource InputLabel}" HorizontalTextAlignment="Center"/>
                    <StackLayout Orientation="Vertical"
                                Grid.Column="0"
                                Style="{StaticResource FormLineContainer}">
                        <Border Stroke="{AppThemeBinding Light=Black, Dark=White}"
                            StrokeThickness="2"
                            Background="Transparent"
                            Padding="0"
                            HorizontalOptions="FillAndExpand" VerticalOptions="FillAndExpand">
                            <Image Source="{Binding Image}" HeightRequest="500" WidthRequest="300"/>
                        </Border>
                        <Button Text="Plese select an image"
                            Margin="0,5"
                            Command="{Binding ImageSelectCommand}" />
                    </StackLayout>
                    <Button Text="Add member to team" WidthRequest="200" Command="{Binding MemberAddingCommand}"/>
                </VerticalStackLayout>
            </Border>
        </StackLayout>
    </ScrollView>
</ContentPage>